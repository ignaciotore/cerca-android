const CACHE='cerca-web-v1';const STATIC=['/','/index.html','/styles.css','/app.js','/pwa.js','/manifest.webmanifest','/icon.svg'];self.addEventListener('install',e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll(STATIC)).then(()=>self.skipWaiting())));self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(k=>Promise.all(k.filter(x=>x!==CACHE).map(x=>caches.delete(x)))).then(()=>self.clients.claim())));self.addEventListener('fetch',e=>{const r=e.request;if(r.method!=='GET')return;const u=new URL(r.url);if(u.origin!==location.origin)return;if(r.mode==='navigate'){e.respondWith(fetch(r).then(x=>{const y=x.clone();caches.open(CACHE).then(c=>c.put(r,y));return x}).catch(()=>caches.match(r).then(x=>x||caches.match('/'))));return}e.respondWith(caches.match(r).then(x=>x||fetch(r).then(res=>{if(res.ok){const y=res.clone();caches.open(CACHE).then(c=>c.put(r,y))}return res})))});
self.addEventListener('push',e=>{let d={};try{d=e.data?e.data.json():{}}catch{};const title=d.title||'🚨 Alerta CERCA';const o={body:d.body||'Una persona de tu Red CERCA necesita ayuda.',icon:'/icon.svg',badge:'/icon.svg',tag:d.emergency_id||'cerca-alert',renotify:true,data:d};e.waitUntil(self.registration.showNotification(title,o))});self.addEventListener('notificationclick',e=>{e.notification.close();const id=e.notification.data?.emergency_id||'';e.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(ws=>{for(const w of ws){if('focus'in w){w.postMessage({type:'open-alert',id});return w.focus()}}return clients.openWindow('/'+(id?'?alert='+encodeURIComponent(id):''))}))});